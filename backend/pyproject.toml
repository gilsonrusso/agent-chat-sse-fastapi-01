[project]
name = "ecommerce-backend"
version = "0.1.0"
description = "Separate E-Commerce API serving product and sales endpoints"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn>=0.34.0",
    "pydantic>=2.0",
]
