[project]
name = "mcp-service"
version = "0.1.0"
description = "MCP Service mapping agent-service OpenAPI spec to tools"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.4.1",
    "httpx>=0.28.1",
    "uvicorn>=0.34.0",
]
